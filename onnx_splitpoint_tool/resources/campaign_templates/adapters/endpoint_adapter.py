"""Replace with the frozen endpoint-family adapter used by the campaign.

This placeholder is deliberately non-executable.  A final campaign must bind
the real adapter implementation here (or update pipeline_contract_sources.yaml
to its real source path) before creating the pipeline contract manifest.
"""

raise RuntimeError("replace_campaign_endpoint_adapter_placeholder")
